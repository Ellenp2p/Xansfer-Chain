# Backend API origin. Leave empty to use Vite dev proxy (/api -> localhost:3001).
# For a production build pointing at a deployed backend:
#   VITE_BACKEND_URL=https://api.xansfer.example.com
VITE_BACKEND_URL=
